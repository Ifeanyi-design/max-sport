
  # User greeting (Copy)

  This is a code bundle for User greeting (Copy). The original project is available at https://www.figma.com/design/rr1D8S4et7IkZOk6u8Ye4f/User-greeting--Copy-.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  